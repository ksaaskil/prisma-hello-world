export interface User {
    id: string
    name: string | null
    password: string
}

export interface Post {
    id: string
    /*
    id: ID!
    title: String!
    published: Boolean!
    author: User
    */
}